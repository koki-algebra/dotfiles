"Disable vi editor compatibility options
set nocompatible

"Display line numbers
set number

"Display the file name being edited in the title bar of the terminal
set title

"Highlight matches to a string search
set hlsearch

"Highlight the cursor line
set cursorline

"Enable command completion in command mode
set wildmenu

"Enable syntax highlighting
syntax enable

"Enable file type detection,
"auto-load plugins for file types,
"enable auto-indentation for file types
filetype plugin indent on
